import template from './template.html';
import controller from './controller';
import './styles.scss';

let loginComponent = {
    bindings: {},
    template,
    controller
};

export default loginComponent;
